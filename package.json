{
  "name": "solid-pod-ecosystem",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n discovery,pod-mgr,data-req -c blue,green,magenta \"npm run dev -w apps/discovery-server\" \"npm run dev -w apps/pod-manager\" \"npm run dev -w apps/data-requester\""
  },
  "devDependencies": {
    "@inrupt/solid-client-authn-node": "^1.12.1",
    "concurrently": "^9.1.0"
  }
}
